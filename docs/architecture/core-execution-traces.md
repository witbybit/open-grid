# Canonical execution traces

Line anchors were checked with `rg` on 2026-07-28. “Decision duplicate?” means another production path makes the same policy decision, not merely that it calls the same method.

## 1. Accepted and rejected `GridApi.setCellValue`

| #   | Hop                                                                                                           | I/O                                                  | State read/write                                                        | Role, faults, diagnostics, duplicate decision                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| 1   | `api/GridApiSurfaces.ts:77 GridApi.setCellValue`                                                              | `(string,string,unknown) -> GridWriteResult`         | none                                                                    | Stable public contract; no duplicate public write API.                                                              |
| 2   | `internal/createGridRuntimeComposition.ts:86 setCellValue`                                                    | API args -> `runtime.setCellValue`                   | none                                                                    | Package boundary forwarding only; necessary stable entrypoint boundary.                                             |
| 3   | `store.ts:426 GridStore.setCellValue`                                                                         | args -> `GridEngine.setCellValue`                    | none                                                                    | Store runtime facade; forwards the stable API into the owned engine.                                                |
| 4   | `engine/GridEngine.ts:1096 setCellValue`                                                                      | args -> domain `GridWriteResult`                     | reads engine capabilities/versions                                      | Canonical command entry; rejected capability/validation is returned before structural write.                        |
| 5   | `engine/GridDomainMutation.ts:568 cellValueExecutor`                                                          | cell mutation -> applied mutation                    | reads row model/column definition; writes only through structural model | Policy/validation boundary; rejected mutation has no rollback because it has not written.                           |
| 6   | `rowModel.ts:ClientStructuralRowModel` structural cell write (call sites `GridDomainMutation.ts:635,809,985`) | write intent -> `RowModelWriteResult`                | writes `RowDataStore`                                                   | Authoritative client storage mutation; no separate renderer write.                                                  |
| 7   | `features/DataMutationController.ts:41 applyStructuralWriteEffects`                                           | `RowModelWriteResult -> StructuralWriteEffectResult` | formula graph/cache and cell-notification set                           | Formula/dependent effect owner; failure is reported by commit isolation after state is committed.                   |
| 8   | `rowModel.ts:1332 reconcileAfterDataWrite`                                                                    | write result + impact -> `RowModelRefreshResult`     | client projection/indexes                                               | Derived projection only.                                                                                            |
| 9   | `engine/GridChangeApplier.ts:357-360` -> `GridEngine.ts:1448-1449 requestRender`                              | commit record -> causal render request               | writes invalidation queue and render-request causal state               | Commit schedules a render request; the later renderer frame is not incorrectly represented as `requestScrollFrame`. |

Depth: 9 typed calls; semantic handoffs: **7** (public API, store facade, command policy, storage, effects, projection, render request). The rejected path stops at hop 5; accepted observer failures cannot roll back committed rows.

## 2. Mixed `GridApi.applyTransaction` (update + remove)

| #   | Hop                                                                                           | I/O                                                      | State read/write                                          | Role, faults/rollback, diagnostics, duplicate decision                                                 |
| --- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| 1   | `api/GridApiSurfaces.ts:57 applyTransaction` -> `internal/createGridRuntimeComposition.ts:66` | `RowDataTransaction<T> -> RowNodeTransaction<T> \| null` | none                                                      | Stable public API, then package-boundary forwarding.                                                   |
| 2   | `engine/GridEngine.ts:769 applyTransaction`                                                   | transaction -> typed row mutation                        | reads capability; no row write                            | Canonical command entry.                                                                               |
| 3   | `engine/GridDomainMutation.ts:862-890 rowTransactionExecutor`                                 | mutation -> prepared snapshot                            | reads `ClientStructuralRowModel`; writes no rows          | Rejects unsupported models before write; captures `RowModelTransactionSnapshot` at `rowModel.ts:1541`. |
| 4   | `rowModel.ts:1283 applyTransactionStructurally`                                               | transaction -> `RowModelWriteResult`                     | writes `RowDataStore` nodes/data                          | Only raw structural write decision.                                                                    |
| 5   | `engine/GridDomainMutation.ts:907-954`                                                        | result -> effects/impact/reconcile/events/history        | writes formula effects, projection, commit history/events | Sole lifecycle owner; recorder captures the same committed domain event.                               |
| 6   | `rowModel.ts:1332 reconcileAfterDataWrite`                                                    | result + `RowWriteImpact` -> refresh result              | writes visual projection                                  | Derived projection; no event decision.                                                                 |
| 7   | `engine/GridDomainMutation.ts:959` -> `rowModel.ts:1552 restoreTransactionSnapshot`           | fault -> restore snapshot                                | restores client storage/projection snapshot               | Rollback uses the same capability; failure is surfaced through the commit result/fault reporter.       |

Depth: **7** calls, semantic handoffs: **5** (public intent, commit policy, storage, effects, projection). The old compatibility shell duplicated hops 4–6; Plan 165 deletes it rather than adding an adapter.

## 3. Sort/filter query change

| #   | Hop                                                                                                                            | I/O                                       | State read/write                                  | Role, faults, diagnostics, duplicate decision                                        |
| --- | ------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------ |
| 1   | `api/GridApiSurfaces.ts:128-129` -> `internal/createGridRuntimeComposition.ts:115-116`                                         | `SortModel/FilterModel -> void`           | none                                              | Stable API and package forwarding.                                                   |
| 2   | `engine/GridEngine.ts:1043/1046 setSortModel/setFilterModel`                                                                   | model -> feature call                     | reads engine lifetime                             | Delegates query policy; it does not mutate state.                                    |
| 3   | `features/GridStateFeatureController.ts:183/221`                                                                               | model -> `GridChange`                     | reads current state; creates typed change         | Sole query-route policy (this corrects the earlier false direct-StateManager trace). |
| 4   | `engine/GridChangeApplier.ts:400 apply` -> `:244 commitState`                                                                  | change -> commit result                   | writes `StateManager` configuration/domain change | Commit ordering and rejection/rollback boundary; recorder sees committed change.     |
| 5   | row-model `refresh` (`rowModel.ts`, `infiniteRowModel.ts:962`, `serverSideRowModel.ts`)                                        | state/query -> `RowModelRefreshResult`    | writes model-specific projection                  | Three real projection implementations; no renderer query policy.                     |
| 6   | `renderer/invalidationManager.ts:233` -> `engine/GridChangeApplier.ts:357-360` -> `engine/RenderRequestCoordinator.ts:request` | invalidation -> `renderInvalidated` event | invalidation queue and causal request state       | Targeted render request; the host/renderer consumes the observable event later.      |

Depth: **6**, semantic handoffs: **5** (API, query policy, commit state, projection, render request). Configuration is state-owned; query model selection is feature-owned; they are intentionally distinct, not duplicate authority.

## 4. Fast scroll then idle repair

| #   | Hop                                                   | I/O                                | State read/write                                      | Role, faults, diagnostics, duplicate decision                     |
| --- | ----------------------------------------------------- | ---------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------- |
| 1   | `renderer/renderEngine.ts:576 onScroll`               | `(top,left,timestamp?) -> void`    | none                                                  | DOM adapter handoff.                                              |
| 2   | `renderer/renderScrollCoordinator.ts:130 onScroll`    | scroll offsets -> viewport request | writes scroll/idle bookkeeping                        | Scroll policy; no second scroll-idle owner.                       |
| 3   | `renderer/frameCoordinator.ts:106 requestScrollFrame` | request -> RAF                     | writes pending RAF/scroll-idle state                  | Sole cancellable frame scheduler; faults are lifecycle-contained. |
| 4   | `renderer/renderViewportCoordinator.ts:22`            | offset -> layout plan              | reads geometry/row projection; writes viewport plan   | Derived viewport boundary.                                        |
| 5   | `renderer/renderPaintCoordinator.ts:39`               | plan -> paint work                 | writes paint epoch/fidelity phase                     | Epoch/phase safety owner; invokes fidelity repair after idle.     |
| 6   | `renderer/rowRenderer.ts` (`recycleViewport`)         | plan -> bound DOM slots            | writes mounted slots/portals via `PortalMountManager` | Physical DOM identity; Flight Recorder observes, never schedules. |

Depth: **6**, semantic handoffs: **5** (input, scroll policy, schedule, viewport, paint/DOM). `GridReplayScheduler` is a deterministic diagnostic seam and is not a duplicate renderer scheduler.

## 5. Host destruction with optional observers and React subscriptions

| #   | Hop                                                                                                               | I/O                                          | State read/write                                                      | Role, faults, diagnostics, duplicate decision                             |
| --- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| 1   | `createGrid.ts:203 destroy`                                                                                       | `() -> void`                                 | reads host lifecycle refs                                             | Public host teardown starts; no rollback after destroy.                   |
| 2   | `persistence/statePersistence.ts` controller `destroy` and workspace destroy                                      | void -> stopped observer                     | writes optional observer lifecycle                                    | Optional-cost boundary; errors must not resurrect runtime.                |
| 3   | runtime `destroy` via `createGrid.ts:208` / `gridHost.ts`                                                         | void -> unmounted runtime                    | writes destroyed lifetime state                                       | Canonical host lifecycle boundary.                                        |
| 4   | `store.ts:1185 unbindRuntimePorts` -> `store/GridStoreHostFacade.ts:75`                                           | binding token -> detached ports              | clears port binding only when token matches                           | Stale-token/fault safety boundary.                                        |
| 5   | `renderer/renderEngine.ts RenderEngine.destroy` -> `renderer/frameCoordinator.ts DefaultFrameCoordinator.destroy` | renderer -> cancelled work                   | cancels RAF/idle, releases renderer                                   | Prevents post-destroy work; teardown continues through isolated failures. |
| 6   | `diagnostics/GridFlightRecorder.ts:133 destroy` / `GridTraceReplay.ts:591 destroy`                                | observer -> stopped                          | clears optional trace/replay state                                    | Diagnostics remain observer-only.                                         |
| 7   | `packages/react/src/GridView.tsx:214-227` mount-effect cleanup                                                    | subscription/host -> unsubscribe and destroy | clears interaction binding, host ref, adapter handle and portal store | Framework boundary; no core state mutation.                               |

Depth: **7**, semantic handoffs: **6** (host, optional observers, runtime, ports, renderer, React). The observable terminal state is no further scheduling, no bound renderer port, and no live React subscription.
