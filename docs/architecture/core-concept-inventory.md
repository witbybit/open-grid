# Core concept inventory

Inventory command (run 2026-07-28):

```powershell
rg -n --glob '!*.test.ts' --glob '*.ts' '^(export )?(abstract )?class [A-Za-z0-9_]*(Controller|Coordinator|Manager|Registry|Bridge|Port|Runtime)\b' packages/core/src
```

It returns **44 definitions** below, with no exclusions and no duplicate rows. Consumer evidence is bounded: the number is the count of production TypeScript files returned by `rg -l --glob '!*.test.ts' --glob '*.ts "\bSYMBOL\b" packages/core/src`, including the definition; named composition/call sites identify the actual boundary. “1 impl” means no separately declared production implementation of the class’s responsibility was found; it does not by itself imply deletion.

Role numbers follow Plan 164: 1 owner, 2 safety boundary, 3 alternative seam, 4 optional boundary, 5 replaceable indirection, 6 duplicate authority, 7 obsolete path.

Plan 167 deleted `GridEngineRenderBridge`: its transaction bracketing converged into `GridEngine`, while queued and synchronous cell notification ownership remains in `CellNotificationController`.

| Definition (path:line)                                                               | Production consumers (bounded evidence)        | Role; state / impl / disabled cost / failure boundary                                     | Recommendation; confidence |
| ------------------------------------------------------------------------------------ | ---------------------------------------------- | ----------------------------------------------------------------------------------------- | -------------------------- |
| `GridCapabilityManager` (`capabilities/GridCapabilityManager.ts:51`)                 | 4 files; `GridEngine` composition/API checks   | 1; capability registry; 1; always-small; rejects unsupported actions                      | keep; high                 |
| `CellNotificationController` (`engine/CellNotificationController.ts:18`)             | 6; `GridEngine`, mutation effects              | 1; subscriptions; 1; always; subscriber error isolation                                   | keep; high                 |
| `ClipboardController` (`features/ClipboardController.ts:39`)                         | 5; feature/API composition                     | 4; clipboard session; 1; disabled when feature absent; browser failure boundary           | keep; high                 |
| `InfiniteRowModelController` (`infiniteRowModel.ts:487`)                             | 12; engine/model selection                     | 3; block/projection state; 3 row projections; absent for client/server; datasource errors | keep; high                 |
| `ColumnFeatureController` (`features/ColumnFeatureController.ts:6`)                  | 5; engine/API composition                      | 1; column feature policy; 1; always; command validation                                   | keep; high                 |
| `GridInsightRegistry` (`insights/GridInsightRegistry.ts:17`)                         | 7; insight modules/engine                      | 4; registered insight layers; 1; feature-disabled cost isolation; layer error isolation   | keep; high                 |
| `GridInteractionController` (`interaction/GridInteractionController.ts:49`)          | 10; engine, renderer, API                      | 2; interaction state; 1; always; DOM/interaction fault boundary                           | keep; high                 |
| `GridConflictManager` (`features/conflict/GridConflictManager.ts:20`)                | 3; conflict feature composition                | 4; conflict queue; 1; absent if disabled; remote-write failure boundary                   | keep; high                 |
| `RenderRequestCoordinator` (`engine/RenderRequestCoordinator.ts:4`)                  | 3; `GridEngine` render requests                | 1; nesting/causal request state; 1; always-small; reentrancy boundary                     | keep; high                 |
| `ColumnAutoSizeController` (`features/ColumnAutoSizeController.ts:31`)               | 3; feature/API composition                     | 4; measurement request state; 1; optional; measurement failure boundary                   | keep; medium               |
| `GridPluginRegistry` (`plugins/GridPluginRegistry.ts:5`)                             | 3; plugin runtime                              | 4; plugins; 1; optional; plugin isolation                                                 | keep; high                 |
| `ClientRowModelController` (`rowModel.ts:968`)                                       | 44; client model use sites                     | 3; client projection; 3 projections; client-only; row data boundary                       | keep; high                 |
| `GridDataIntegrityManager` (`features/dataIntegrity/GridDataIntegrityManager.ts:64`) | 5; integrity modules/engine                    | 4; integrity layer state; 1; optional; validation error isolation                         | keep; high                 |
| `EditingFeatureController` (`features/EditingFeatureController.ts:31`)               | 4; feature/API composition                     | 1; edit session; 1; always; validation/cancel boundary                                    | keep; high                 |
| `DataMutationController` (`features/DataMutationController.ts:38`)                   | 4; `GridEngine` injects at `GridEngine.ts:498` | 1; formula/dependent effects; 1; always; formula failure isolation                        | keep; high                 |
| `ColumnInteractionController` (`renderer/columnInteractionController.ts:78`)         | 6; `RenderEngine`                              | 2; column pointer state; 1; always; DOM boundary                                          | keep; high                 |
| `ServerSideRowModelController` (`serverSideRowModel.ts:388`)                         | 8; engine/model selection                      | 3; server projection; 3 projections; server-only; fetch failure boundary                  | keep; high                 |
| `GridStateFeatureController` (`features/GridStateFeatureController.ts:17`)           | 3; `GridEngine` at `:1043/:1046`               | 1; query/state command policy; 1; always; input normalization                             | keep; high                 |
| `CustomRendererManager` (`renderer/customRendererManager.ts:64`)                     | 4; renderer composition                        | 2; custom renderer instances; 1; optional renderer cost; user renderer fault boundary     | keep; high                 |
| `DomCellRendererManager` (`renderer/domCellRendererManager.ts:39`)                   | 3; renderer composition                        | 2; DOM renderer handles; 1; always; DOM lifecycle boundary                                | keep; high                 |
| `FillDragController` (`renderer/fillDragController.ts:26`)                           | 3; `RenderEngine`                              | 4; fill drag session; 1; optional interaction; pointer fault boundary                     | keep; high                 |
| `StateManager` (`state/StateManager.ts:5`)                                           | 12; `GridChangeApplier` at `:244`              | 1; grid config; 1; always; commit rollback boundary                                       | keep; high                 |
| `DefaultFrameCoordinator` (`renderer/frameCoordinator.ts:75`)                        | 6; `RenderEngine`/scroll coordinator           | 1; RAF/idle state; 1; always; cancel-on-destroy boundary                                  | keep; high                 |
| `ViewportController` (`viewportController.ts:8`)                                     | 3; engine/renderer projection                  | 1; viewport state; 1; always; bounds boundary                                             | keep; medium               |
| `GeometryController` (`renderer/geometryController.ts:3`)                            | 6; renderer composition                        | 1; geometry cache; 1; always; layout boundary                                             | keep; high                 |
| `RowDependencyRegistry` (`rows/rowMutationClassifier.ts:60`)                         | 4; mutation classification                     | 1; dependent row graph; 1; feature-conditioned; dependency-boundary                       | keep; high                 |
| `RowSelectionFeatureController` (`features/RowSelectionFeatureController.ts:7`)      | 4; feature/API composition                     | 1; selection policy; 1; always; selection validation                                      | keep; high                 |
| `RowDragController` (`features/RowDragController.ts:26`)                             | 4; feature/API composition                     | 4; drag state; 1; disabled feature; drag failure boundary                                 | keep; high                 |
| `ColumnTopologyCoordinator` (`renderer/columnTopologyCoordinator.ts:29`)             | 3; `RenderEngine`                              | 1; column topology projection; 1; always; layout phase boundary                           | keep; medium               |
| `HeaderMenuController` (`renderer/headerMenuController.ts:15`)                       | 3; `RenderEngine`                              | 4; menu lifecycle; 1; optional UI; DOM boundary                                           | keep; high                 |
| `GroupingFeatureController` (`features/GroupingFeatureController.ts:16`)             | 4; feature/API composition                     | 1; group state commands; 1; feature-dependent; input validation                           | keep; high                 |
| `GridDiffManager` (`features/diff/GridDiffManager.ts:16`)                            | 3; diff insight composition                    | 4; diff layer/cache; 1; disabled feature has no layer work; layer fault isolation         | keep; high                 |
| `InvalidationManager` (`renderer/invalidationManager.ts:233`)                        | 8; `GridChangeApplier`/renderer                | 1; invalidation queue; 1; always; normalize/apply boundary                                | keep; high                 |
| `LayoutTransitionController` (`renderer/layoutTransitionController.ts:58`)           | 7; renderer composition                        | 2; transition epoch; 1; always-small; transition failure/teardown boundary                | keep; medium               |
| `PinnedContainerManager` (`renderer/pinnedContainerManager.ts:4`)                    | 2; row renderer                                | 1; pinned DOM containers; 1; pinned mode cost; DOM boundary                               | keep; high                 |
| `PortalMountManager` (`renderer/portalMountManager.ts:87`)                           | 17; row renderer/React mount path              | 2; mount tokens/identity; 1; React optional; stale-release safety                         | keep; high                 |
| `RenderInvalidationCoordinator` (`renderer/RenderInvalidationCoordinator.ts:26`)     | 4; `RenderEngine`                              | 1; render invalidation projection; 1; always; render phase boundary                       | keep; medium               |
| `RenderPaintCoordinator` (`renderer/renderPaintCoordinator.ts:39`)                   | 3; `RenderEngine`                              | 1; paint epochs/fidelity; 1; always; stale-paint boundary                                 | keep; high                 |
| `RenderScrollCoordinator` (`renderer/renderScrollCoordinator.ts:106`)                | 3; `RenderEngine`                              | 1; scroll/idle bookkeeping; 1; always; scroll scheduling boundary                         | keep; high                 |
| `RenderViewportCoordinator` (`renderer/renderViewportCoordinator.ts:22`)             | 3; `RenderEngine`                              | 1; viewport/layout plan; 1; always; bounds/scroll boundary                                | keep; high                 |
| `RowRendererRuntimeBridge` (`renderer/rowRendererRuntime.ts:116`)                    | 4; `rowRenderer.ts` owner                      | 2; hot-path dependency/lifecycle projection; 1; always-small; portal/focus boundary       | keep; medium               |
| `SelectionPaintManager` (`renderer/selectionPaintManager.ts:19`)                     | 6; renderer/selection paths                    | 1; selection paint cache; 1; selection-dependent; paint boundary                          | keep; high                 |
| `ThemeManager` (`renderer/themes.ts:567`)                                            | 5; store/renderer                              | 2; theme lifecycle/cache; 1; always-small; DOM/theme boundary                             | keep; high                 |
| `ValidationTooltipController` (`renderer/ValidationTooltipController.ts:6`)          | 3; renderer/integrity integration              | 4; tooltip lifecycle; 1; only validation UI; DOM teardown boundary                        | keep; high                 |

## Non-class concepts that carry complexity

| Family             | Definition / consumers                                                                                             | Role and recommendation                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------- |
| Cache              | `InfiniteBlockCache` (`infiniteRowModel.ts:233`), client indexes, formula cache                                    | 1/3; retain: source/projection-specific caches have different invalidation owners.                          |
| Schedulers         | `DefaultGridScheduler` (`renderer/gridScheduler.ts:27`), `DefaultFrameCoordinator`, injected `GridReplayScheduler` | 1/2; retain: browser frame work and deterministic replay have different nondeterminism boundaries.          |
| Versions           | `GridEngine` domain versions, `RenderPaintCoordinator` epoch, renderer freshness stamps                            | 1; retain: each guards a different state transition; never substitute one for another.                      |
| Mutation protocols | `GridChange`, `GridDomainMutation`, `RowModelWriteResult`, `StructuralWriteEffectResult`, `RowNodeTransaction`     | 1; retain canonical commit protocols; Plan 165 deletes only `TransactionalRowModel` compatibility protocol. |
| Identity protocols | row id (`RowDataStore`), column instance identity, cell identity, portal token                                     | 1/2; retain: writers and failure domains differ.                                                            |
| Port binding       | `RuntimePortBinding`, `GridStoreHostFacade`                                                                        | 2; retain: generation token prevents stale renderer attach/detach.                                          |

## Candidate ranking and semantic guard decision

| Rank | Slice                                     | Delete / expected scope                                                                                                                                                                                                 | Safety and coverage                                                                                                                  | Result               |
| ---- | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------- |
| 1    | Row-transaction compatibility shell       | `TransactionalRowModel`, cast/getter, fallback branch, client `applyTransaction`, duplicated collector; expected 1 protocol + 2 helpers + 1 method, roughly 70–120 LOC across `rowModel.ts` and `GridDomainMutation.ts` | focused transaction, rollback, formula, event/history, server/infinite rejection, API/pack/bench/recorder gates; one-commit rollback | **Select: Plan 165** |
| 2    | Public API/runtime composition forwarding | likely no safe deletion; 285-line composition boundary                                                                                                                                                                  | stable package API and host lifecycle depend on it                                                                                   | reject               |
| 3    | Renderer coordinator merger               | would move rather than delete several concepts                                                                                                                                                                          | phase/epoch/scroll/DOM isolation not proven duplicate                                                                                | reject               |

No new semantic guard is justified: the deletion is enforced more durably by absence of the deleted exported/internal capability plus behavior tests. Import/source-string guards would recreate the brittleness this program is intended to remove.
